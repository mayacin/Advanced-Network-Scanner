# Advanced Network Scanner 🔎

This is a Python-based network scanner that detects active devices, scans ports, and identifies operating systems using Scapy and Nmap.

## Features
- Host discovery
- Port scanning
- OS detection
- Service identification

## Installation
```bash
sudo apt update && sudo apt install nmap python3-pip -y
pip install scapy python-nmap
